<Window x:Class="MVVMAwesoniumPOC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HTLM5 vs WPF" Loaded="Window_Loaded" Height="350" Width="525" xmlns:awe="http://schemas.awesomium.com/winfx">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!--Source="C:\Users\David\Documents\Visual Studio 2010\Projects\MVVMAwesoniumPOC\MVVMAwesoniumPOC\src\index.html"-->
        <awe:WebControl    x:Name="wcBrowser" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch">
            <awe:WebControl.ContextMenu>
                <ContextMenu Visibility="Collapsed"/>
            </awe:WebControl.ContextMenu>
        </awe:WebControl>
        <StackPanel Grid.Column="1">
            <TextBox Text="{Binding Path=Name,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding Path=LastName}"/>
            <TextBlock Text="{Binding Path=Local.City}"/>
            <ListBox ItemsSource="{Binding Path=Skills}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path=Type}"/>
                            <TextBlock Text="{Binding Path=Name}"/>

                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Button" Height="23" Click="Button_Click" Width="75" />
        </StackPanel>
    </Grid>
</Window>
